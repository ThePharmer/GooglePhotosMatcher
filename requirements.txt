Pillow>=9.0.0
PySimpleGUI>=4.60.0
piexif>=1.1.3
win32-setctime>=1.1.0; sys_platform == 'win32'

# Optional dependencies for extended format support:
# pillow-heif>=0.13.0  # For HEIC/HEIF support (iPhone photos)
# ffmpeg (system package)  # For video metadata (MP4, MOV, etc.)
